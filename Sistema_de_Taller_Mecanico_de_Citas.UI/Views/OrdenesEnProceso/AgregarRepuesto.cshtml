@model dynamic

@{
    ViewData["Title"] = "Agregar Repuesto";
}

<style>
    html, body {
        height: 100%;
        margin: 0;
    }

    body {
        display: flex;
        justify-content: center;
        align-items: start;
        background-image: url('/img/fondo.png');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        font-family: 'Segoe UI', sans-serif;
        padding: 2rem;
    }

    .form-container {
        background-color: white;
        border-radius: 15px;
        box-shadow: 0 0 15px rgba(0,0,0,0.3);
        padding: 30px 25px;
        width: 100%;
        max-width: 850px;
        max-height: 95vh;
        overflow-y: auto;
        color: #212529;
    }

    table {
        width: 100%;
        margin-bottom: 2rem;
        cursor: pointer;
    }

    th, td {
        padding: 0.5rem;
        text-align: left;
        border-bottom: 1px solid #ccc;
    }

    th {
        background-color: #f2f2f2;
    }

    tr:hover {
        background-color: #f5f5f5;
    }

    .form-label {
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: #333333;
    }

    .form-control {
        font-size: 1rem;
    }
</style>

<div class="form-container">
    <h2 class="text-center text-warning fw-bold mb-4">Agregar Repuesto a Orden</h2>

    <h5 class="mb-3">Seleccione un repuesto de la siguiente lista:</h5>
    <table class="table table-bordered" id="tablaRepuestos">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Precio Unitario</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in ViewBag.Repuestos)
            {
                <tr onclick="seleccionarRepuesto('@item.Id', '@item.Nombre', @item.Precio)">
                    <td>@item.Nombre</td>
                    <td>₡@item.Precio.ToString("N2")</td>
                </tr>
            }
        </tbody>
    </table>

    <form asp-action="AgregarRepuesto" method="post">
        <input type="hidden" name="idOrden" value="@ViewBag.IdOrden" />
        <input type="hidden" name="idRepuesto" id="idRepuesto" />

        <div class="mb-3">
            <label class="form-label">Nombre:</label>
            <input type="text" id="nombreRepuesto" class="form-control" readonly />
        </div>

        <div class="mb-3">
            <label class="form-label">Precio Unitario:</label>
            <input type="text" id="precioRepuesto" class="form-control" readonly />
        </div>

        <div class="mb-3">
            <label for="cantidad" class="form-label">Cantidad:</label>
            <input type="number" name="cantidad" id="cantidad" class="form-control" min="1" required />
        </div>

        <div class="mb-3" id="totalContainer" style="display: none;">
            <label class="form-label">Total:</label>
            <input type="text" id="total" name="total" class="form-control" readonly />
        </div>

        <div class="d-flex justify-content-between mt-4">
            <button type="submit" class="btn btn-success fw-bold px-4" id="btnAgregar" disabled>Agregar</button>
            <a asp-action="Index" asp-controller="OrdenesEnProceso" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>

<script>
    let precioSeleccionado = 0;

    function seleccionarRepuesto(id, nombre, precio) {
        document.getElementById("idRepuesto").value = id;
        document.getElementById("nombreRepuesto").value = nombre;
        document.getElementById("precioRepuesto").value = "₡" + parseFloat(precio).toFixed(2);
        precioSeleccionado = parseFloat(precio);
        document.getElementById("btnAgregar").disabled = false;

        document.getElementById("cantidad").value = "";
        document.getElementById("total").value = "";
        document.getElementById("totalContainer").style.display = "none";
    }

    document.getElementById("cantidad").addEventListener("input", function () {
        const cantidad = parseInt(this.value);
        if (!isNaN(cantidad) && precioSeleccionado > 0) {
            const total = cantidad * precioSeleccionado;
            document.getElementById("total").value = "₡" + total.toFixed(2);
            document.getElementById("totalContainer").style.display = "block";
        } else {
            document.getElementById("totalContainer").style.display = "none";
        }
    });
</script>
